# AGENTS.md

## Project overview

**Super Duper Scrum Poker** is a real-time, collaborative planning poker web app for agile teams. A facilitator (or any participant) creates a room, shares an invitation link containing a short 6-char session ID, and teammates join from any device by entering a name. Players vote in secret on story-point estimates, the votes are revealed, the average is shown, and the team repeats rounds until they reach consensus.

- Purpose: run remote or in-room estimation sessions (story point voting) without a whiteboard or physical cards.
- Audience: agile/scrum teams and their facilitators; no accounts or logins (anonymous Firebase auth).
- The UI is multilingual: Spanish by default, with English and French.

## Tech stack

- **Frontend**: React 18 + TypeScript (~5.6), Vite 5, Tailwind CSS 4, `lucide-react` icons, `clsx` + `tailwind-merge`.
- **Backend**: Firebase — anonymous auth + Cloud Firestore.
- **Testing**: Playwright + `playwright-bdd` (Gherkin scenarios) against the Firebase Emulator Suite (auth 9099, Firestore 8085); `firebase-tools` is a devDependency.
- **Tooling**: ESLint 9 (flat config, `typescript-eslint`, `react-hooks`, `react-refresh`), GitHub Actions (CI + deploy to GitHub Pages).
- **i18n**: manual — no i18n library; `t()` over `src/i18n/translations.ts`.

## Architecture & structure

- Single-page React app with no client routing — `react-router-dom` is a dependency but unused; keep it that way (YAGNI).
- `src/App.tsx` shows `WelcomeScreen` (lobby: create a session, or join via the `/room/<ID>` route) vs `PokerTable` (game) based on `SessionContext` state. The URL is kept in sync with the session via `history.replaceState` (no client routing).
- One Firestore doc per game at `sessions/{id}` (a `players` array plus `revealed`/`average`); session IDs are 6-char client-generated strings.

```
├── src/                        # React app
│   ├── App.tsx                 # Switches between lobby (WelcomeScreen) and game (PokerTable)
│   ├── main.tsx                # Entry point
│   ├── index.css               # Tailwind entry
│   ├── components/             # PokerTable, WelcomeScreen, VotingCards, Results, LanguageSelector
│   ├── context/                # SessionContext (Firestore sync, game actions), LanguageContext
│   ├── services/               # firebase.ts — SDK init (db, auth)
│   ├── i18n/                   # translations.ts — manual t() dictionary (es, en, fr)
│   ├── types/                  # Shared types (Session, Player, Vote, context types)
│   └── utils/                  # URL helpers (room route parse/build)
├── tests/                      # E2E BDD suite
│   ├── features/*.feature      # Gherkin scenarios, one file per domain (session, voting, results, language)
│   ├── steps/*.steps.ts        # Step definitions, one file per domain
│   ├── steps/fixtures.ts       # TestWorld + world fixture (one isolated BrowserContext per player)
│   ├── contract/               # (empty, reserved)
│   ├── integration/            # (empty, reserved)
│   ├── global-setup.ts         # Starts Firebase emulators, frees ports
│   └── global-teardown.ts      # Kills emulator process group, clears ports
├── docs/agent/                 # Detailed AI-facing rules by domain (referenced below)
├── .features-gen/              # Specs generated by bddgen (gitignored, never hand-edited)
├── .opencode/                  # Task specs & roadmap (SDD)
│   ├── roadmap.md              # Long-term development plan (task index)
│   └── specs/<task>/           # Per-task: acceptance.md, decisions.md, progress.md
├── .github/workflows/          # ci.yml (build+lint, e2e), deploy.yml (GitHub Pages)
├── firebase.json               # Emulator config (auth 9099, firestore 8085)
├── firestore.rules             # Firestore security rules
├── playwright.config.ts        # bddgen features/steps globs, webServer, emulators setup
├── vite.config.ts / tsconfig*.json / tailwind.config.js / postcss.config.js / eslint.config.js
├── .env.example / .env.test    # Env var template / dummy values for the emulators
└── test-artifacts/             # Playwright HTML report + results (generated)
```

## How to work (task workflow)

The `.feature` files are the **living documentation** of the application and the **single source of truth** for its behavior. If a behavior isn't in a `.feature`, it doesn't exist.

Implementation of a spec follows this ATDD lifecycle, from branch to merge:

1. **Branch** — create a branch from `main` (kebab-case, prefixed `feat/`, `fix/`, `chore/`, ...).
2. **Feature first** — define or extend the Gherkin scenarios (`tests/features/<domain>.feature`) and step definitions (`tests/steps/`) based on the spec's acceptance criteria. The new/modified scenarios **must fail** when run (`npm run test -- -g "title"`).
3. **Implement** — build the feature, iterating until all quality gates pass. Minor in-path refactoring is allowed; larger refactors are separate tasks.
4. **Validation** *(checkpoint)* — present the implementation to the user. If changes are needed: return to step 2 (if the features need redefinition) or step 3 (if only implementation changes). If approved: continue.
5. **Commit & push** — make logical commits with Conventional Commit messages. **Each commit must be a functional state of the application with all quality gates green.** Push to the branch.
6. **Pull request** — open the PR to `main` (`gh pr create`), then enable automerge (`gh pr merge <number> --auto`). Wait for the CI pipeline.
7. **Pipeline** — if the pipeline **fails**: inform the user of the problem and a proposed fix, then wait for direction. If the pipeline **passes**: the PR merges automatically (rebase/ff) and the implementation phase is complete.

**Quality gates** — a feature is "passing" only when all of the following are green:
- `npm run lint` (ESLint 9)
- `npm run build` (`tsc -b && vite build`)
- `npm run test` (full BDD E2E suite)

**Rules:**
- **Always confirm first (no exceptions):** anything touching **security** — `firestore.rules` / security rules.
- **Language:** all code, documentation, and commit messages are written in **English**.
- Editing a `.feature` or step file invalidates generated specs; `npm run test` always regenerates them.
- Every feature is validated against the ATDD principle before implementation starts; complexity beyond the acceptance scenarios must be justified in writing.

## Git & CI

- Conventional Commits (`feat(scope): ...`, `fix(ci): ...`, `chore(deps): ...`, ...).
- Branches in kebab-case (`feat/...`, `fix/...`); pull requests target `main`.
- CI on PR to `main`: build+lint (Node 20) and the full BDD E2E suite (official Playwright container, Java 17, serial workers, 2 retries, `forbidOnly`).
- Push to `main` deploys `dist/` to GitHub Pages (Firebase config injected from repo secrets).
- Never commit secrets: Firebase config comes from `VITE_FIREBASE_*` vars in `.env` / `.env.local` (gitignored) — copy `.env.example`.

## Commands

- `npm run test` — full BDD E2E suite. Always use this (or `npm run ui` for Playwright UI mode); it runs `bddgen` first. Never run `npx playwright test` directly: spec files are generated, not committed.
- Single scenario: `npm run test -- -g "scenario title"`.
- `npm run lint` — ESLint 9 (flat config in `eslint.config.js`).
- `npm run build` — `tsc -b && vite build`. There is no separate typecheck script; `tsc -b` inside build is it.
- `npm run dev` — Vite dev server.

## Environment

- Firebase config comes from `VITE_FIREBASE_*` vars in `.env` / `.env.local` (gitignored) — copy `.env.example`. `.env.test` holds dummy values for the emulators.
- Emulator ports (from `firebase.json`, single-project mode): auth 9099, firestore 8085.
- CI (`.github/workflows/ci.yml`): build+lint job on Node 20; e2e job runs inside the official Playwright container with Java 17 and `firebase-tools` installed, `npm run test` with `CI=true` (serial workers, 2 retries).
- Deploy (`.github/workflows/deploy.yml`): push to `main` builds and deploys `dist/` to GitHub Pages; Firebase config is injected from repo secrets. The build also emits `dist/404.html` (a copy of `index.html`) so GitHub Pages serves the SPA for `/room/<ID>` deep links.

## Agent documentation

Detailed rules by domain. Read the relevant one when touching that part of the codebase:

- [Code style](docs/agent/code-style.md) — TypeScript, React, Tailwind, clean-code, architecture rules.
- [Testing (ATDD / BDD E2E)](docs/agent/testing.md) — features, steps, fixtures, emulators, known flakiness.
- [Internationalization](docs/agent/i18n.md) — manual `t()` convention across `es` / `en` / `fr`.

## Documentation

- `README.md` — product description (currently minimal; keep updated as the app grows).
- `.env.example` — required `VITE_FIREBASE_*` environment variables.
- `firestore.rules` — Firestore security rules.
- `test-artifacts/` — generated Playwright HTML report and results (not committed).
